# Protocol rules

Protocols can define automatic suggestions and annotation constraints. In the
reference application, generated suggestions require an explicit decision before
they enter the retained annotations. Violations can produce warning messages;
the application does not certify clinical correctness.

EAF imports preserve mapped annotations without applying automatic rules.
See [protocols](../study-setup/protocol-schema.md) and [annotation workflow](workflow.md).
