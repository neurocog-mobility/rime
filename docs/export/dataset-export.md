# Dataset export

The current desktop has no dataset/clip export dialog. Source dataset organization is separate from the RIME record specification; `.rime` does not prescribe a BIDS directory layout. Core export utilities are separate from the desktop workflow.

See [saving and comparing measurements](../clinical-analysis/clinical-metrics.md).
